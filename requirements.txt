# requirements.txt for ego_exo_analysis_26
#
# Contains only packages directly required by this project.
# Transitive dependencies are resolved automatically by pip.
#
# Install order matters -- run setup.sh or follow these steps manually:
#
#   1. pip install "setuptools==81.0.0" "wheel==0.47.0"
#   2. pip install torch torchvision          # installs latest with CUDA bundled
#   3. pip install -r requirements.txt
#   4. pip install -e ./r3m                   # submodule editable install
#   5. pip install -e ./vip                   # submodule editable install
#
# setuptools==81.0.0 is pinned -- this specific version correctly registers
# pkg_resources, which is required by gdown and r3m at import time on Python 3.12.

# =============================================================================
# Simulation environment
# =============================================================================
metaworld==3.0.0          # eval scripts use ML1 API -- do not upgrade beyond this
mujoco==3.7.0             # physics engine; pinned to match working venv

# =============================================================================
# Core ML / numerics  (torch and torchvision installed separately -- see above)
# =============================================================================
numpy==1.26.4
scipy==1.17.1
Pillow==9.1.0             # intentionally 9.1.0 not 9.0.1 -- VIP requires 9.0.1 but 9.1.0
                          # fixes a downstream bug while remaining compatible with VIP
opencv-python==4.11.0.86

# =============================================================================
# R3M and VIP dependencies
# (also declared in submodule setup files; listed here to guarantee versions
#  are correct before the editable installs run)
# =============================================================================
gdown==4.4.0              # model weight downloader; pinned -- newer versions drop pkg_resources
hydra-core==1.1.1         # VIP config system; do not upgrade (3.12 patch in setup.sh covers it)
omegaconf==2.1.1          # hydra dependency
antlr4-python3-runtime==4.8  # hydra dependency

# =============================================================================
# DROID fine-tuning (TFRecord streaming)
# =============================================================================
tensorflow==2.21.0
tensorflow-datasets==4.9.9
tfrecord==1.14.6
protobuf==6.31.1          # pinned -- tf 2.21 requires >=6.31.1

# =============================================================================
# R3M language loss (DistilBERT)
# =============================================================================
transformers==5.5.4
tokenizers==0.22.2

# =============================================================================
# Training utilities
# =============================================================================
tqdm==4.67.3
wandb==0.26.0

# =============================================================================
# Dataset download tools
# =============================================================================
ego4d==1.7.3              # provides egoexo CLI for Ego-Exo4D download
awscli==1.44.84           # for Ego-Exo4D AWS credentials
gsutil==5.36              # for DROID Google Cloud Storage download